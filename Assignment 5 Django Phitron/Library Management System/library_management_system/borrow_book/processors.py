# from users.models import UserAccountModel

# def balance(request):
#     balance_obj = UserAccountModel.objects.filter(user=request.user).first()
#     print(balance_obj.get_balance())
#     return {'balance': balance_obj.get_balance()}